[tool.poetry]
name = "poetry-web"
version = "0.1.0"
description = ""
authors = ["PatrickSalmi <patrick.salmi@helsinki.fi>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
